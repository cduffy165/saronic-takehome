# Copy to .env for local (host-side) commands: alembic, make seed, manual scripts.
# The compose stack sets its own container env vars directly in compose.yaml and
# does not read this file.
DATABASE_URL=postgresql+psycopg://factory:factory@localhost:5432/factory
